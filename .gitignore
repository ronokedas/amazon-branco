# Intencionalmente vazio.
#
# Este projeto publica todos os arquivos no GitHub, inclusive .env,
# dependencias, uploads, documentos, backups, logs e arquivos de runtime.
# Use `git add --all` ou `scripts/publicar_tudo_github.ps1` para que nenhum
# arquivo novo seja omitido.
